import React, { Component } from 'react';

// Add CSS of this component
import './header.scss';

export default class Header extends Component {
  render() {
    return (
      <div className="header text-center">
        Header
      </div>
    );
  }
}
